# A Small But Opinionated Vimrc

## Getting Started

Fork this repository and clone into `~/.vim`:

> Back up your .vim folder if it already exists

```
   $ git clone https://github.com/Tattomoosa/starter-vimrc ~/.vim
```
